<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Playlist</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="/edit-playlist.css">
</head>

<body>
    <%- include('partials/header') %>
        <div class="playlist-container">
            <h1>Edit Playlist Item</h1>
            <form action="/edit-playlist/<%= playlist._id %>" method="POST">
                <div class="form-group">
                    <label for="title">Title:</label>
                    <input type="text" id="title" name="title" value="<%= playlist.title %>" required>
                </div>
                <div class="form-group">
                    <label for="artist">Artist:</label>
                    <input type="text" id="artist" name="artist" value="<%= playlist.artist %>" required>
                </div>
                <div class="form-group">
                    <label for="genre">Genre:</label>
                    <input type="text" id="genre" name="genre" value="<%= playlist.genre %>" required>
                </div>
                <div class="btn-container">
                    <button type="submit" class="edit-btn">Update</button>
                    <button type="button" class="cancel-btn"
                        onclick="window.location.href='/view-playlist'">Cancel</button>
                </div>
            </form>
        </div>
</body>

</html>